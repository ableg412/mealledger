# Frontend environment variables.
# Copy to `.env.local` for local dev.
#
# In local dev, leave this BLANK — Vite's proxy (vite.config.ts) forwards
#   /api/* to http://localhost:4000.
#
# In production, Render fills this in with the deployed API URL.
VITE_API_BASE_URL=
