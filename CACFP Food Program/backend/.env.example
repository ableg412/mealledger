# Backend environment variables.
# Copy to `.env` for local dev. Render injects these via render.yaml in production.

# Port the Express server listens on.
PORT=4000

# Comma-separated list of allowed origins for CORS.
# In production, Render auto-fills this with the frontend URL.
CORS_ORIGIN=http://localhost:5173
